version: '3.8'

services:
  koreader-sync:
    build: .
    container_name: koreader-sync
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./koreader.db:/app/koreader.db
